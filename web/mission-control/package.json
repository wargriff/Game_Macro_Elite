{
  "name": "xclicker-mission-control",
  "version": "2.1.0",
  "private": true,
  "description": "Mission Control web — dev optionnel. En production, HTML statique servi par Sidecar (port 17840).",
  "scripts": {
    "dev": "npx --yes serve . -p 5173",
    "build": "echo Static build: copier index.html vers web/mission-control/"
  }
}
